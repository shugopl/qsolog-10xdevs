Step 9: Implement frontend foundation in /frontend.

Requirements:
- Angular 21, Angular Material, routing:
  /login, /register, /qso, /qso/new, /qso/:id/edit, /stats, /ai/reports
- Auth:
  - AuthService calling backend /auth endpoints.
  - Interceptor attaches Bearer token.
  - Guard protects routes.
  - Token storage: in-memory + sessionStorage fallback (document tradeoffs). Short-lived tokens assumed.
- Forms:
  - Material form fields, validators.
- Environment config for backend base URL.

DO NOT implement QSO screens yet; just auth + layout shell + navigation.

Tests:
- basic unit tests for AuthService (mock HttpClient).
STOP after Step 9.
