Step 6: Implement export endpoints.

GET /api/v1/export/adif?from=&to=
- Produce a valid minimal ADIF file with fields at least:
  CALL, QSO_DATE, TIME_ON, BAND, MODE
  plus optional if present: FREQ, RST_SENT, RST_RCVD, QTH, GRIDSQUARE, COMMENT/NOTES, QSL statuses (if you map them, otherwise vendor fields).
- Return as text/plain or application/octet-stream with Content-Disposition attachment.

GET /api/v1/export/csv?from=&to=
- Basic CSV for humans (Excel-friendly).

Requirements:
- Stream output reactively (Flux<DataBuffer> or similar) if feasible; otherwise keep it simple but correct.
- Ensure exports are scoped to user_id and date range.

Tests:
- Export contains header and at least one QSO serialized correctly.

ADIF export rules:
- Always emit MODE from AdifMode.
- If submode is present, emit SUBMODE accordingly.
- If customMode is present:
  - emit MODE=DATA, omit SUBMODE
  - emit vendor field: APP_QSOLOG_CUSTOMMODE with value customMode
Vendor field format:
  <APP_QSOLOG_CUSTOMMODE:{len}>{value}

  Add ADIF export regression tests for mode/submode/customMode mapping.

Test cases (at least these):
1) UI "CW":
   stored mode=CW, submode=null, customMode=null
   ADIF must contain: <MODE:2>CW
   ADIF must NOT contain SUBMODE for this QSO.

2) UI "SSB":
   stored mode=SSB
   ADIF must contain: <MODE:3>SSB

3) UI "PSK31":
   stored mode=PSK, submode=PSK31
   ADIF must contain: <MODE:3>PSK and <SUBMODE:5>PSK31

4) UI "FT8":
   stored mode=MFSK, submode=FT8
   ADIF must contain: <MODE:4>MFSK and <SUBMODE:3>FT8

5) UI "Other/Custom" customMode="VARAC":
   stored mode=DATA, submode=null, customMode="VARAC"
   ADIF must contain: <MODE:4>DATA
   ADIF must contain vendor field: <APP_QSOLOG_CUSTOMMODE:5>VARAC
   ADIF must NOT contain SUBMODE for this QSO.

Also verify minimal required ADIF fields exist in every QSO record:
CALL, QSO_DATE, TIME_ON, BAND, MODE and at least BAND present (FREQ optional).


STOP after Step 6.
