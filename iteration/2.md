Step 2: Add PostgreSQL schema via Flyway and wire R2DBC config.

Schema:
- users:
  id UUID PK, email unique, username unique, password_hash, role (ADMIN/OPERATOR),
  created_at, updated_at
- qso:
  id UUID PK, user_id UUID FK(users.id),
  their_callsign (CALL),
  qso_date (date, UTC),
  time_on (time, UTC),
  band (varchar) REQUIRED,
  frequency_khz (numeric) OPTIONAL,
  mode (enum-like varchar) REQUIRED,
  rst_sent (varchar) optional, rst_recv (varchar) optional,
  qth (varchar) optional, grid_square (varchar) optional,
  notes (text) optional,
  qsl_status (varchar) NOT NULL default 'NONE',
  lotw_status (varchar) NOT NULL default 'UNKNOWN',
  eqsl_status (varchar) NOT NULL default 'UNKNOWN',
  created_at, updated_at
Indexes:
  qso(user_id, qso_date desc)
  qso(user_id, their_callsign)
  qso(user_id, band)

- ai_report_history (for later AI helper history):
  id UUID PK, user_id FK, from_date, to_date, language (PL/EN), content (text), created_at

Implement Flyway migrations under backend/src/main/resources/db/migration:
- V1__init.sql

R2DBC:
- Configure connection properties in application.yaml.

Deliverables:
- SQL migrations
- Domain enums scaffolds for Role, Mode, QslStatus, LotwStatus, EqslStatus (do not overbuild yet).
- Update README: how to run Postgres via docker-compose (compose will come later but mention env vars).

Verification:
- Add integration test using Testcontainers Postgres that boots Spring context and verifies Flyway ran (table exists).

Add to qso table:
- submode varchar null
- custom_mode varchar null

Create enums:
Band (ADIF band names):
160m,80m,60m,40m,30m,20m,17m,15m,12m,10m,6m,4m,2m,1.25m,70cm,33cm,23cm,13cm,9cm,6cm,3cm,1.25cm,6mm,4mm,2.5mm,2mm,1mm

AdifMode core:
CW, SSB, AM, FM, RTTY, PSK, MFSK, DATA

AdifSubmode core:
FT8, FT4, JS8, PSK31
(optional extras: JT65, JT9, OLIVIA, CONTESTIA, PSK63, PSK125)

Add domain validation rules:
- if submode in {FT8,FT4,JS8} then mode must be MFSK
- if submode in {PSK31,PSK63,PSK125} then mode must be PSK
- if custom_mode not null then mode must be DATA and submode must be null

STOP after Step 2.
