Step 5: Implement stats summary endpoint.

GET /api/v1/stats/summary?from=YYYY-MM-DD&to=YYYY-MM-DD
Response:
{
  countsByBand: [{band,countAll,countConfirmed}],
  countsByMode: [{mode,countAll,countConfirmed}],
  countsByDay:  [{date,countAll,countConfirmed}],
  totals: {all, confirmed}
}

Confirmed definition:
- confirmed if qslStatus==CONFIRMED OR lotwStatus==CONFIRMED OR eqslStatus==CONFIRMED

Implementation:
- Use reactive DB queries (can be custom SQL in repository).
- All scoped by user_id and date range.

Tests:
- insert sample QSOs, verify aggregates.

STOP after Step 5.
