Step 11: DevOps hardening.

Docker:
- /docker/docker-compose.yml with postgres 16+ and volume.
- backend/Dockerfile multi-stage, non-root, exposes 8080.
- frontend/Dockerfile build + nginx serve, SPA fallback config.

CI/CD (GitHub Actions):
- backend: mvn test
- frontend: npm ci, ng test (headless) or minimal test run, ng build
- optional: integration tests with Testcontainers.
- produce artifacts (optional) and build docker images (optional, no push required).

Update README:
- local run step-by-step
- env vars (DB, JWT_SECRET, HAMQTH creds, OPENAI optional)
STOP after Step 11.
