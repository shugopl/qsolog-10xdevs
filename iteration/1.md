Step 1: Implement backend foundation in /backend with Java 25 + Spring Boot 4 WebFlux.

Requirements:
- Create Maven project (wrapper optional) with dependencies:
  WebFlux, Spring Security (for later), Spring Data R2DBC, R2DBC Postgres,
  Flyway + JDBC Postgres driver (only for migrations),
  Validation, Actuator, springdoc-openapi-starter-webflux-ui,
  Test deps: JUnit5, Reactor Test, Testcontainers (postgres), Spring Boot Test.
- Provide application.yaml for dev with placeholders:
  DB url/user/pass, JWT secret placeholder, CORS allowed origins for localhost:4200.
- Implement global error handling (RFC7807 ProblemDetails-like) for validation/errors.
- Actuator health endpoint enabled.
- OpenAPI UI available (document how to reach it).
- DO NOT implement auth logic yet; just scaffold config placeholders.
- Ensure app starts with no DB (but DB connectivity will be used later).
- Add a simple /api/v1/ping endpoint returning {status:"ok"}.

Deliverables:
- backend/pom.xml, src main/test structure, minimal controller, error handler, config classes.
- Update top-level README with backend run instructions.

Verification:
- Run: mvn test and mvn spring-boot:run (or equivalent) and fix issues.
STOP after completing Step 1 and printing file tree.

