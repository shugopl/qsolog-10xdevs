Step 3: Implement authentication & authorization.

Endpoints:
POST /api/v1/auth/register  {email, username, password} -> create user with BCrypt hash, default role OPERATOR unless specified (ADMIN creation only via config/seed)
POST /api/v1/auth/login     {usernameOrEmail, password} -> returns {accessToken, tokenType:"Bearer", expiresInSeconds}
GET  /api/v1/auth/me        -> returns {id, email, username, role}

Rules:
- Passwords stored only as BCrypt hash.
- JWT contains: sub=userId, username, role, exp.
- Secure all /api/v1/qso/**, /api/v1/stats/**, /api/v1/export/**, /api/v1/lookup/**, /api/v1/ai/** by default.
- Allow anonymous only for /api/v1/ping and /api/v1/auth/** and swagger/openapi endpoints.
- CORS: allow http://localhost:4200 for dev.
- Provide a simple seed option: if env ADMIN_BOOTSTRAP=true and ADMIN credentials provided, create ADMIN user at startup if missing (idempotent). Document in README.

Deliverables:
- Security config (Reactive), JWT utility, auth service/usecase layer separation (SOLID / clean architecture).
- Tests:
  - register/login works
  - protected endpoint returns 401 without token
  - role is in /me response

Verification:
- mvn test
STOP after Step 3.
